namespace Claw;

/// <summary>
/// Define os tipos de cursor padrão.
/// </summary>
public enum Cursor
{
	Default, Text, Wait,Crosshair, Progress,
	SizeNWSE, SizeNESW, SizeHorizontal, SizeVertical, Move,
	NotAllowed, Pointer
}
